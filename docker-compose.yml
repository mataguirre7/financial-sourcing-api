services:
  db:
    image: postgres:16-alpine
    restart: always
    ports:
      - 2160:5432
    environment:
      POSTGRES_PASSWORD: 123qwe
      POSTGRES_USER: postgres
      POSTGRES_DB: financial-sourcing-api
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data: